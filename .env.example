# Copy this file to .env (do NOT commit .env)
# Example DATABASE_URL for SQL Server using pyodbc driver (URL-encode special chars in password)
# Replace PASSWORD with your actual password (or use the exact encoded value below)

# Example (ODBC Driver 17):
# DATABASE_URL="mssql+pyodbc://USER:PASSWORD@HOST:1433/DATABASE?driver=ODBC+Driver+17+for+SQL+Server"

# Example using the connection you provided (password URL-encoded):
# Note: this example contains the password you shared URL-encoded. Store secrets securely.
DATABASE_URL="mssql+pyodbc://sa:%24ys112177%23@alfacode.com.mx:1432/alfacodeuniversoactivo?driver=ODBC+Driver+17+for+SQL+Server"

# Optional token for write-protection
UNIVERSO_API_TOKEN=
